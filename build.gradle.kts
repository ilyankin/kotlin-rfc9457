// Intentionally empty. Shared configuration lives in `build-logic` as convention plugins applied per
// module; a `subprojects { }` / `allprojects { }` block here would reintroduce the 40 Isolated
// Projects violations that motivated moving away from it.
